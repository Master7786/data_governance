[project]
name = "data_governance"
version = "0.0.1"
requires-python = ">=3.10,<3.13"

dependencies = [
    "streamlit>=1.40.0"
]

authors = [
    { name = "sample@example.com" }
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
