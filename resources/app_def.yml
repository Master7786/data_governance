resources:
  apps:
    sample_app:
      name: "sample-app"
      description: "sample app"

      # The location of the source code for the app.
      source_code_path: ../src/app/ui
      resources:
        - name: clientid
          secret:
            scope: security
            key: clientid
            permission: READ
        - name: secret
          secret:
            scope: security
            key: secret
            permission: READ
